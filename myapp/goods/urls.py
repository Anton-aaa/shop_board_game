from django.urls import path, include
# from rest_framework import routers
# from .views import (
    # GoodsListView,
    # GoodsDetailView,
    # GoodsUpdateView,
    # GoodsCreateView,
# )

# router = routers.SimpleRouter()
# router.register("goods", MyUserModelViewSet)

urlpatterns = [
    # path('', GoodsListView.as_view(), name="goods_list"),
    # path('<int:pk>/', GoodsDetailView.as_view(), name="goods_detail"),
    # path('update/<int:pk>/', GoodsUpdateView.as_view(), name="goods_update"),
    # path('create/', GoodsCreateView.as_view(), name="goods_create"),
    # path('', include(router.urls)),
]